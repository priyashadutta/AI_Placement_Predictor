# AI_Placement_Predictor

Built Logistic Regression model on dataset with 100 rows and 3 features for placement prediction Performed preprocessing, feature scaling, and model training using Scikit-learn Evaluated model performance using accuracy score and saved model using Pickle
